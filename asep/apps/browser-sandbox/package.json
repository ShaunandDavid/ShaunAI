{
  "name": "@shaunai/browser-sandbox",
  "version": "0.2.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "postinstall": "playwright install --with-deps"
  },
  "dependencies": {
    "@shaunai/shared": "workspace:*",
    "fastify": "4.26.2",
    "playwright": "1.47.2",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "tsx": "4.16.2",
    "vitest": "2.0.5"
  }
}
